Разовая проверка: сверить число статей в папках AlphaXiv.

Сделай ОДИН вызов:
    list_library(include_papers=false)

Напечатай ровно таблицу: имя папки и paper_count, только для этих папок:
Optimization/muon, Optimization/optimization_base, Optimization/zero-order,
LLM/scaling_laws, LLM/quantization_llm, LLM/low_bit_training, LLM/architectures,
RL/RL, RL/rlhf, PEFT/lora_base, Want to read.

Формат строки: `<имя папки>: <paper_count>`. Ничего не добавляй, не комментируй,
статьи не перечисляй. Если папка не нашлась — напиши `<имя>: НЕ НАЙДЕНА`.
